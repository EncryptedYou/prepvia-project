import {createClient} from "@supabase/supabase-js"; import {createIcons,ArrowLeft,LockKeyhole,ShieldCheck,Check} from "lucide";
const supabase=createClient(import.meta.env.VITE_SUPABASE_URL,import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY);const message=document.getElementById("message");function show(text,type="error"){message.textContent=text;message.className=`message ${type}`}
document.getElementById("resetForm").addEventListener("submit",async e=>{e.preventDefault();const password=document.getElementById("password").value,confirm=document.getElementById("confirm").value;if(password.length<8)return show("Password must contain at least 8 characters.");if(password!==confirm)return show("Passwords do not match.");const button=document.getElementById("submitButton");button.disabled=true;const {error}=await supabase.auth.updateUser({password});button.disabled=false;if(error)return show(error.message);show("Password updated successfully. Redirecting to sign in…","success");setTimeout(()=>location.href="/auth.html",1400)});createIcons({icons:{ArrowLeft,LockKeyhole,ShieldCheck,Check}});
